rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Each signed-in player has exactly one stats doc, keyed by their own
    // uid — they can read and write only their own.
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
